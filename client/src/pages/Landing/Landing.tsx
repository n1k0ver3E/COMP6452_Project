import React, { FC } from 'react'
import MainNavbar from '../../components/MainNavBar'

const Landing: FC = () => {
  return (
    <div className="App">
      <MainNavbar />
    </div>
  )
}

export default Landing
